{% extends '::base.html.twig' %}

{% block body %}
    <div class="text-center">
        formulaire:
        {{form(form)}}
        <br>
        -------------
        <br>
        {% if( name is not null) %}
        nom : {{name}} <br>
        email: {{email}} <br>
        objet: {{objet}} <br>
        {% endif %}
    </div>
{%endblock%}
